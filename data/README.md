This folder contains data that is not used during the main site build.

For example, `./navs` contains JSON files that represent the navigation tree structure for all published packages in the registry.
